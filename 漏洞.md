# 漏洞


## 双平台
### VULN-005   network.request / downloadFile SSRF（⽆ URL 校验） 双平台(TODO已修改android)
### VULN-006   ⽆证书固定 + 全局明⽂流量（MITM ⻛险放⼤器）(TODO还差，限定证书指纹)
### VULN-008   跨游戏共享存储⽆访问控制 (已修改)
### VULN-010   ⽀付及授权参数 Log.i 明⽂泄露 (已修改)  参考文档docs/init-log-usage.md


## 仅 Android

### VULN-007   登录 Token 明文存储(已修改)
### VULN-011   动态 SO 远程加载⽆完整性校验（Native RCE）(已修改)
### VULN-012   Bilibili API 凭据硬编码 (已修改)
### VULN-013   Zip Slip 路径穿越（任意⽂件写⼊） (已修改)
### VULN-014   Manifest 过度权限声明 (已修改)


## 仅iOS

### VULN-015   WKWebView allowUniversalAccessFromFileURLs 潜在设置 仅 iOS(已修改)
### VULN-018：`createWorker` 远程 JS 加载无校验 (已修改)




## 由BDMiniGame SDK处理的


#### VULN-004   openMiniGame JSAPI 接受外部 package_url
#### VULN-009   游戏包 MD5 校验缺陷（双平台）
#### VULN-016   debugEnabled 开关可在⽣产环境开启 Web Inspector仅 iOS


## 有疑问的

### VULN-001   DeepLink 任意游戏包加载 + V8 JSAPI 全量⽆鉴权（完整攻击链)
待确认
### VULN-002   OAuth 授权码⽆限制调⽤（getAuthCode 鉴权绕过
待确认
### VULN-003   同进程游戏间⽆隔离（内存/存储/⽂件共享） (双平台)
修改成本过大，android目前都是基于单进程的实现。IOS无法实现多进程
 
### VULN-017   window.prompt 同步 JSAPI 通道可被注⼊利⽤ (仅 iOS)
在  BGameCSPromptRouter 处理层增加调⽤来源校验：验证调⽤帧的 URL 是否属于可信游戏包域（blfile:// 或 lbg:// 协议，且 gameId 匹配当前会话）；
WKScriptMessageHandlerWithReply （iOS 14+），完全废弃 window.prompt 拦截⽅式               (WKScriptMessageHandlerWithReply异步替代不了同步window.prompt)
对所有经 prompt 通道的 JSAPI 调⽤增加与异步通道相同的权限校验，不因通道差异⽽绕过。  (这个是底层API 数量太多了 不可能一一加权限 而且很多是底层API)